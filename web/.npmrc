# Fail fast on unsupported Node versions instead of hitting a cryptic
# "webidl.util.markAsUncloneable is not a function" error inside vitest/jsdom.
# The toolchain (jsdom 30, undici 8, vite 8) requires Node >= 22.22.2.
engine-strict=true
