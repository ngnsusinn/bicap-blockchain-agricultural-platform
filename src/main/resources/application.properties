# Server Port Configuration
server.port=${SERVER_PORT:8080}

# Database Configuration (MySQL)
<<<<<<< Updated upstream
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/bicap_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root}
=======
spring.datasource.url=jdbc:mysql://free02.123host.vn:3306/roacqgfa_bicap?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=roacqgfa_bicap
spring.datasource.password=laptrinhjavahahaha
>>>>>>> Stashed changes
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
# - Production uses ddl-auto=validate so silent schema mutation (from ddl-auto=update)
#   can never happen against a real database. Demo/test profiles override to 'update'
#   so the H2 in-memory schema still builds itself.
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:validate}
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ------------------------------------------------------------------------
# Caching Configuration (Redis)
# ------------------------------------------------------------------------
spring.data.redis.host=${SPRING_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_REDIS_PORT:6379}
spring.data.redis.password=${SPRING_REDIS_PASSWORD:}
spring.data.redis.ssl.enabled=${SPRING_REDIS_SSL:false}

# ------------------------------------------------------------------------
# Application & JWT Configuration
# ------------------------------------------------------------------------
spring.application.name=bicap-blockchain-agricultural-platform
# REQUIRED at deploy time: the app FAILS TO START if JWT_SECRET is unset. Never use
# a default signing key — a known key lets anyone forge tokens for any account.
app.jwt.secret=${JWT_SECRET}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# ------------------------------------------------------------------------
# Payment Gateway (Sepay) Configuration
# ------------------------------------------------------------------------
sepay.account-no=${SEPAY_ACCOUNT_NO:123456789}
sepay.bank-name=${SEPAY_BANK_NAME:MBBank}
# REQUIRED: startup fails if unset. The placeholder / empty values are rejected too,
# so a forgeable webhook cannot be reached with the shipped defaults.
sepay.api-key=${SEPAY_API_KEY}
