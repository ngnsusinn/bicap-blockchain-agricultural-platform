name: Java & Node.js CI

run-name: Build & Test triggered by ${{ github.actor }}

on:
  push:
    branches: [ "main", "feature/*" ]
  pull_request:
    branches: [ "main" ]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # Unified React app: portal ("/") + admin dashboard ("/admin") in one build.
  web-ci:
    name: Web CI (Lint, Test & Build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          # web/ requires Node >= 22.22.2: jsdom 30 (^22.22.2 || ^24.15.0) and
          # undici 8 (>=22.19.0) both dropped Node 20, which fails vitest with
          # "webidl.util.markAsUncloneable is not a function".
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

  backend-ci:
    name: Backend CI (Test & Package)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'corretto'
          java-version: '21'
          cache: 'maven'

      - name: Run Unit Tests
        run: mvn clean test

      - name: Package with Maven
        run: mvn package -DskipTests

      - name: Upload Build Artifact (JAR)
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: backend/target/*.jar
          retention-days: 1
